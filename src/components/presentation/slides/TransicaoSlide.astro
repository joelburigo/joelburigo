---
import Container from '../../ui/Container.astro'
import { Icon } from 'astro-icon/components'
---

<div class="h-screen bg-dark flex items-center justify-center overflow-hidden">
  <Container>
    <div class="max-w-6xl mx-auto px-4">
      
      <div class="text-center mb-8">
        <h2 class="font-display text-5xl font-bold text-white mb-4">
          Escolha Seu <span class="text-lime">Caminho</span>
        </h2>
        <p class="text-xl text-gray-400">
          Baseado no que conversamos, clique na opção que mais faz sentido
        </p>
      </div>

      <!-- Forquilha de Caminhos -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">
        
        <!-- Caminho DIY - VSS -->
        <button 
          @click="goTo(10)"
          class="group rounded-xl border-2 border-lime/30 bg-lime/5 p-6 text-center hover:border-lime/60 hover:bg-lime/10 transition-all cursor-pointer"
        >
          <div class="mb-4">
            <Icon name="lucide:graduation-cap" class="h-14 w-14 text-lime mx-auto group-hover:scale-110 transition-transform" />
          </div>
          <h3 class="text-2xl font-bold text-white mb-3">Vendas Sem Segredos</h3>
          <p class="text-sm text-gray-300 mb-4">
            Quer aprender e implementar sozinho.<br/>
            Tem tempo e quer autonomia.
          </p>
          <div class="rounded-lg bg-lime/20 px-4 py-2 mb-3">
            <span class="text-xs font-bold text-lime uppercase tracking-wider">DIY - Faça Você Mesmo</span>
          </div>
          <div class="flex items-center justify-center gap-2 text-lime font-semibold">
            <span>Ver Detalhes</span>
            <Icon name="lucide:arrow-right" class="h-5 w-5" />
          </div>
        </button>

        <!-- Caminho DFY - Services -->
        <button 
          @click="goTo(14)"
          class="group rounded-xl border-2 border-blue-500/30 bg-blue-500/5 p-6 text-center hover:border-blue-500/60 hover:bg-blue-500/10 transition-all cursor-pointer"
        >
          <div class="mb-4">
            <Icon name="lucide:rocket" class="h-14 w-14 text-blue-400 mx-auto group-hover:scale-110 transition-transform" />
          </div>
          <h3 class="text-2xl font-bold text-white mb-3">Services</h3>
          <p class="text-sm text-gray-300 mb-4">
            Tem orçamento mas não tem tempo.<br/>
            Quer que façamos por você.
          </p>
          <div class="rounded-lg bg-blue-900/40 px-4 py-2 mb-3">
            <span class="text-xs font-bold text-blue-400 uppercase tracking-wider">DFY - Fazemos Por Você</span>
          </div>
          <div class="flex items-center justify-center gap-2 text-blue-400 font-semibold">
            <span>Ver Detalhes</span>
            <Icon name="lucide:arrow-right" class="h-5 w-5" />
          </div>
        </button>

        <!-- Caminho Advisory -->
        <button 
          @click="goTo(18)"
          class="group rounded-xl border-2 border-purple-500/30 bg-purple-500/5 p-6 text-center hover:border-purple-500/60 hover:bg-purple-500/10 transition-all cursor-pointer"
        >
          <div class="mb-4">
            <Icon name="lucide:compass" class="h-14 w-14 text-purple-400 mx-auto group-hover:scale-110 transition-transform" />
          </div>
          <h3 class="text-2xl font-bold text-white mb-3">Advisory</h3>
          <p class="text-sm text-gray-300 mb-4">
            Já tem time e estrutura.<br/>
            Precisa de direção estratégica.
          </p>
          <div class="rounded-lg bg-purple-900/40 px-4 py-2 mb-3">
            <span class="text-xs font-bold text-purple-400 uppercase tracking-wider">Conselho Estratégico</span>
          </div>
          <div class="flex items-center justify-center gap-2 text-purple-400 font-semibold">
            <span>Ver Detalhes</span>
            <Icon name="lucide:arrow-right" class="h-5 w-5" />
          </div>
        </button>

      </div>

      <!-- Mensagem Final -->
      <div class="text-center">
        <div class="inline-block rounded-xl bg-white/5 border border-white/10 px-6 py-3">
          <p class="text-sm text-gray-300">
            <Icon name="lucide:mouse-pointer-click" class="inline h-4 w-4 text-lime mr-1" />
            <strong class="text-white">Clique no caminho escolhido</strong> para ver os detalhes
          </p>
        </div>
      </div>

    </div>
  </Container>
</div>
