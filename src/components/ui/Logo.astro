---
interface Props {
  class?: string
}

const { class: className = '' } = Astro.props
---

<span class={`font-display inline-flex items-baseline text-xl tracking-[-0.04em] ${className}`}>
  <span class="text-[color:var(--jb-cream)]">JOEL</span><span class="text-[color:var(--jb-fg-muted)] mx-1">|</span><span class="text-[color:var(--jb-fire)]">BURIGO</span>
</span>
