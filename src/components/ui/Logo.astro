---
interface Props {
  class?: string
}

const { class: className = '' } = Astro.props
---

<span class={`font-display text-2xl tracking-tight ${className}`}>
  <span class="font-black text-white">JOEL</span><span class="font-medium text-lime">BURIGO</span>
</span>
